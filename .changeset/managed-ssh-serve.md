---
"@zswarm/core": patch
"@zswarm/cli": patch
"@zswarm/mcp": patch
---

Add ssh:// serve targets: process-owned SSH LocalForward, probeServe before ops, CLI dispose and MCP tunnel reuse. host:port and tcp:// stay compatible.
