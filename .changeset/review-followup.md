---
"@zswarm/core": patch
"@zswarm/cli": patch
"@zswarm/mcp": patch
"zswarm": patch
---

Review follow-up: gate release on master CI, ship policy env vars, locate MCP without a tracked dist, keep wasm in lockstep, and fix wait clocks, empty sessions, SSH quoting, and Pi call timeouts.
